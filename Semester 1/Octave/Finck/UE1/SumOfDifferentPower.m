% sum of different power function% author : Daniel Griesser function [f,g] = SumOfDifferentPower(x)  % input:  % x - vector in column form   % output:  % f - function value [1x1]   % g - gradient vector [nx1]    % check input  n = size(x, 1);  if n == 1    fprintf('input is row vector, transform it into column vector \n')    x = x';     n = size(x, 1);  endif    % function value  f = 0;  for i = 1:(n-1)    f = f + (abs(x(i))^(i+1));  endfor    % gradient vector   g = zeros(n, 1); % create n x 1 matrix filled with 0s  for i = 1:n    g(i) = (i+1) * abs(x(i))^i;  endfor  endfunction